# dsh-qqbot-bridge 项目规范与代理工作手册

本文档是 dsh-qqbot-bridge 项目的核心开发规范。**任何 AI 代理（包括主代理、子代理、后续接手的会话代理）在进入本项目工作时，必须严格遵守以下准则。**

> 本规范沿用 `dsh-napcat-bridge/AGENTS.md`（已对齐 DSH 0.1.5 的成熟项目），并针对本项目「QQ 官方 Bot API + 远程控制 DSH 会话」的定位做了适配。

---

## 1. 代理启动与工作区对齐规范

每位代理在进入本项目开始执行任何任务前，必须首先完成工作区状态对齐：
1. **工作区状态核对**：执行 `git status` 与 `git log -n 5`，快速核对当前工作区状态、最近提交历史以及是否有未提交的修改，确保在干净且明确的代码基础上开展工作；
2. **按需查阅历史归档**：进入工作区时不强制通读全量需求与清单文档；如需了解历史需求背景、协议基线细节或已验收功能，可按需查阅 `docs` 目录下的文档，其中 `docs/方向总纲-*.md` 是**唯一的方向基准**。

### 1.1 docs 目录规范（产出文档统一存放处）
- **`docs/` 目录是项目内所有文档的固定存放处**：需求文档、规格(Spec)文档、Checklist、调研纪要、踩坑考证、任务包、交付报告等，一律归档到 `docs/`（按需分子目录，如 `docs/archive/<主题>_<日期>/`）。
- 代理产出的任何过程性/结论性文档，**必须写入 `docs/`**（落盘到项目内），而非散落在对话或系统盘；便于后续代理与用户查验。
- 关键交付物（方向总纲、任务包、探针结论）必须**呈交用户查验**后才能放行下一阶段。

---

## 2. 决策、事实准确性与沟通原则

### 2.1 事实准确性与严谨调研原则（严禁凭空编造）
- **严禁凭空臆造**：严禁凭主观印象推测任何接口签名、协议字段、类型定义或依赖包行为；
- **确切信息优先**：涉及第三方库（如 DeepSeek Harness 各包、Cordis 等）或外部协议（如 QQ 开放平台 Bot API v2）时，必须通过深入查阅本地 `node_modules` 实际源码/类型声明文件（`.d.ts`）或通过 Web Search 检索官方确切文档；
- **查证前严禁假定已知**：在未实际拿到确切文档或查验源码之前，严禁直接对用户声称已知，必须基于确凿证据开展设计、编码与汇报。

### 2.2 证据分级标注（本项目的硬性要求）
本项目的设计与汇报中，任何结论必须标注证据等级，**严禁把推断包装成事实**：
- **【文档明写】**：QQ 官方文档/DSH 源码 `.d.ts` 中可逐字引用的原文，必须附**官方文档链接**或**仓库内 file:line**；
- **【实测】**：在真实环境跑出来的观测结果，必须附**探针产物路径**（如 `logs/probe/E2-*.jsonl`）；
- **【推断】**：由文档/源码推导但未被明写、未被实测的结论，**必须显式标注【推断】**，且必须进入 `docs/任务包-*探针*` 待验证清单；
- **推翻已有结论时**：必须明确说明原结论错在哪、新证据是什么，并在 `docs/` 留下更正记录（例：本项目方向讨论中曾把「流式只占 1 个回复位」当作事实陈述，后更正为【推断】）。

### 2.3 用户决策制（严禁擅自拍脑袋）
- **用户决策制**：所有未经明确定义的需求、接口设计选型、行为逻辑变更、重要架构调整，**严禁代理自行拍脑袋决定**；
- **主动请示**：遇到边界情况、协议歧义或方案权衡时，必须向用户阐明背景、备选方案及各自优缺点，**获得用户明确确认后再行实施**；
- **范围红线（用户已拍板，严禁自行扩展）**：
  1. **仅单聊 C2C**——群聊、频道、频道私信一律不做（用户判定：群聊引入复杂度且不安全）；
  2. **只做桥接**——记忆、用户画像、人格注入、主动回复/潜水冒泡、表情回应、贴表情、群管，全部不做；
  3. **不自研鉴权层**——依赖 QQ 官方 openid 身份与好友关系；严禁发明 token/签名/自建鉴权体系；
  4. **不做额度防御逻辑**——额度相关限制以真机实测为准（见方向总纲 §5.2），严禁在代码里堆未经验证的防御分支。

### 2.4 复用优先原则（用户拍板的铁律）

> **用户原话**：*"真开始执行这个项目的时候记得从两个已有项目找能复用什么，而不是完全自己新建……两个旧项目已有的实现优先 copy 过来适配这个项目的模型，因为两个项目的实现都是实际验证过可行的，无非就是改一下逻辑，删一些两个项目特有的业务。已有的部分不是重复造轮子。"*

**强制流程**：在写任何新实现之前，必须先检索 `~/dsh-napcat-bridge`（已对齐 DSH 0.1.5，**首选参考**）与 `~/nyagent`（唯一走 QQ 官方 Bot API 的项目），确认是否已有等价实现。

- **已有 ⇒ copy 过来适配，严禁重写。**
- 允许的适配动作：改 import 路径与包名、改配置/常量/命名空间、按本项目模型调整接口与数据结构、**删掉旧项目专有业务**。
- **不允许**的适配理由："风格不喜欢""想写得更干净""想自己设计一遍"——这些一律不作为重写依据。
- **复用映射表必须落盘并持续维护**：`docs/方向总纲-*.md` §7；每个复用点记录 `源文件:行号 → 目标文件 → 适配动作`。
- **copy 来的代码同样要过 §3.1 装配验证**：不得因为"来源项目验证过"就跳过在本项目装配路径（boot 挂载、设置卡片、工具注册、事件钩子）上的闭环验证。

**必须重写的例外**（须逐条列出理由并落盘，不得口头带过）：

1. **协议层不兼容**：OneBot/反向 WS、CQ 码、`reply`/`at` 段、QQ 号做 ID、`message_id` 当整数主键、WSL↔Windows 路径翻译；
2. **旧实现已被证实失效**：如 nyagent 的 `assistant/chunk` 流式源（DSH v2 不存在该事件，整条流式链路从不触发）；
3. **旧实现违反官方约束**：如 nyagent 同时下发 `content` 与 `markdown`、以 `file_data` base64 调富媒体上传接口。

**去债不去功能**——copy 时必须剥离下列已知技术债，但**保留其功能逻辑**：

| 禁止照抄 | 出处 | 替代做法 |
|---|---|---|
| 猴补 `sessionController.prompt` | napcat `gateway/session.ts:150-178` | 用官方服务契约，不改写他方服务 |
| 猴补 `agentDefaultModel.saveSelection` + 模块级全局可变状态 | napcat `commands/index.ts:66-108` | 不维护插件自有模型状态（D26） |
| `ctx.get('x') \|\| (ctx as any).x` 双兜底 | 两个项目遍布 | 显式声明 `inject` 与真实 `.d.ts` 类型 |
| `agents.resume().catch(() => agents.create({sessionId}))` | nyagent `qq-gateway/session.ts:442-452` | 遵守 D30：如实报错，**绝不降级新建** |
| 死代码 / noop 导出 / v2 预留空方法 | napcat `tools/index.ts:708`、`prompt/dynamic.ts:14-18`、`server.ts:359-389` | 不搬；本项目不新增死代码 |
| 超千行单体与混合职责的单函数 | napcat `src/index.ts:358-795` | 拆入站管线（见方向总纲 §6） |

---

## 3. 测试驱动开发 (TDD) 与测试保护红线

- **严格遵循 TDD 流程**：实现或重构功能时，先行编写契约测试，再逐步实现功能；
- **相关测试通过性要求**：与当前修改无关的测试不强求通过（见下条红线），但**与当前修改直接相关的测试用例在最终验收前必须 100% 通过**；
- **严禁为了通过测试而篡改测试用例**：
  - 测试用例是契约与质量红线，**代理无权自行修改测试用例**；
  - 若发现测试用例存在事实性错误或协议理解偏差，**必须先向用户汇报并说明原因，经用户明确确认同意后方可修改**。

### 3.1 禁止"造桩自测自嗨"（严禁以单元测试替代装配验证）
- **单测通过不等于功能完成**：严禁仅凭"模块自建 mock 桩 + 单测全绿"即宣告功能完成——单元测试只能证明零件自身行为，不能证明功能在生产装配路径（boot 挂载、设置卡片、agent 工具注册、事件钩子、运行时接线）上真实可达；
- **交付前必须验证装配闭环**：每个功能交付前须验证"零件 → 装配 → 运行路径"完整闭环，相关接线必须有契约测试（如对真实 `bootDshQqbotBridge` 装配的断言）或真机验证；mock 桩仅允许用于隔离外部依赖（网络、QQ 开放平台、第三方服务），**不允许替代装配验证**；
- **接线缺口必须如实上报**：发现未装配插件、零调用函数、死代码、断链等，必须列入交付报告，**严禁以"单测全绿"掩盖功能实际不可用**。

### 3.2 不相关测试的通过性不设限（红了如实报告，严禁为全绿造假）
- **与当前修改无关的测试变红，属正常可接受状态**：允许红线存在、不强求通过、**严禁"顺手"修绿**——修复属于独立任务，超出本次修改范围时必须先向用户确认后再实施；
- **严禁为了"测试全绿"或交付好看而造假**，包括但不限于：篡改测试用例、放宽/删减断言、删除失败用例、以 mock 绕过真实路径、或调整被测代码回避失败；
- **与当前修改直接相关的测试**，在最终验收前必须 100% 通过（仅限真实装配路径上的相关契约测试）。

### 3.3 能力边界诚实汇报（做不到就明说）
- 功能未实现、未接线、未验证（如缺真机/集成验证）时，必须如实汇报**"没做完 / 没接上 / 需要真机验证"**，严禁用"已完成""已通过测试"等措辞掩盖；
- 交付报告与 commit 信息必须与实际代码一致，**禁止声称未落地的改动**。

### 3.4 契约测试先行与红线正当性（禁止为转绿偷跑实现或造桩自证）
- **红线测试是先行契约的正当期望状态**：编写指向尚未实现功能的契约测试时，该测试保持红色（失败）是正当、合理且符合 TDD 设计预期的状态，绝非交付缺陷；
- **严禁为了"测试全绿"而违规操作**：
  1. 严禁在测试文件内复制实现逻辑来"自我验证"或"自测自嗨"；
  2. 严禁在契约测试阶段提前实现 `src/` 业务逻辑（实现严格属于后续各功能阶段任务包）；
  3. 严禁擅自修改、放宽断言或删除该测试用例；
- **探针阶段的允许产物范围**：`scripts/`（探针脚本）、`logs/`（探针产物）、`docs/`（结论纪要），
  外加**从旧项目 copy 适配的装配助手 `src/boot.ts`**（属脚手架，非业务逻辑）与
  `package.json` / `tsconfig*.json` / `cordis.patch.yml` 等工程配置。
  **协议内核、命令系统、流式桥接、消息组装等业务逻辑一律不得在探针结论之前进入 `src/`。**
- **自然转绿原则**：红着的契约测试必须且只能在对应后续阶段的功能开发完成、真实装配接线并通过验证后，自然转为绿色。

---

## 4. Git 提交规范

### 4.1 提交前检查清单
在执行 `git commit` 之前，必须执行以下检查：
1. `git log -n 5`：确认最近提交记录及提交信息风格；
2. `git status`：确认工作区修改范围；
3. `git diff`：细致审查修改的代码内容，确认没有误改、没有残留的调试日志或临时文件；
4. `pnpm typecheck` & `pnpm test`：确保编译通过且相关测试全部通过。

> **⚠️ 真机交付前强制 build（沿用 napcat PF-001 实测翻车教训，2026-08-30）：** link 部署时 DSH 通过
> `package.json` `main → dist/` 加载编译产物。若只改 `src/` 并提交、却漏跑 `pnpm build`，则 DSH 仍跑
> **旧 dist**，新逻辑不进生产路径 —— 「单测全绿（`test:quick` 只 `vitest run` 不 build）+ 真机失效」正是此根因。
> **任何需要真机验证的交付，提交前必须执行 `pnpm build` 并确认 `dist/` 内容包含本次改动**
> （如 `grep -c "新逻辑关键词" dist/<对应文件>.js`）。

### 4.2 提交范围与独立性
- **严格原子化提交**：只提交自己当前任务修改的那一部分文件，**严禁带入无关文件或全局临时变更**；
- **阶段性提交**：每个子任务或里程碑阶段开发测试完成后，必须立即执行 Git 提交。

### 4.3 提交信息格式
统一采用 **`<type>: <中文标题>`** 格式，例如：
- `feat: 实现 QQ 单聊 WebSocket 网关连接与心跳保持机制`
- `fix: 修复流式分片前缀不一致导致的 40007 中断`
- `test: 增加会话切换与工作区枚举契约测试用例`
- `docs: 归档第一期真机探针结论`
- `chore: 初始化项目结构与依赖配置`

常用 type 类型：
- `feat`: 新增功能/插件模块
- `fix`: 修复 bug
- `test`: 增加或修改测试用例
- `docs`: 文档变更 (archive/markdown)
- `refactor`: 重构代码（不改变现有功能的代码结构调整）
- `chore`: 构建配置、依赖变更、辅助工具等

---

## 5. 多子代理并行协作规范

为了保障高效且安全地多子代理并行执行，必须遵守以下铁律：
1. **文件所有权严格隔离（File Ownership Isolation）**：
   - 派发并行子代理时，**严禁多个子代理同时修改同一个文件**；
   - 必须按模块/文件切分独立职责（例如：代理 A 负责 `qq/client.ts`，代理 B 负责 `qq/stream.ts`，代理 C 负责 `qq/markdown.ts`）；
2. **类型与契约先行**：
   - 跨模块共享的公共类型定义（`src/types/`）与工具类（`src/utils/`）必须先确立并稳定，再派发下游实现代理；
3. **清晰的任务与边界约束**：
   - 给每个子代理的 Prompt 必须包含：
     - **目标职责**（做什么）；
     - **可编辑文件清单**（只能修改哪些文件，严禁触碰其他文件）；
     - **依赖服务与接口**（调用什么接口）；
     - **验收标准与测试用例**（如何验证）。